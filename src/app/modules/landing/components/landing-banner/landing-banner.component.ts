import { Component, OnInit } from '@angular/core';import { getDummyProfile }   from '@shared/utils/profile.utils';import { getDummyNft }       from '@shared/utils/nft.utils';import { Router }            from '@angular/router';@Component({  selector: 'cf-landing-banner',  templateUrl: './landing-banner.component.html',  styleUrls: ['./landing-banner.component.scss']})export class LandingBannerComponent implements OnInit {  constructor(    private router: Router  ) { }  ngOnInit(): void {}  dummyNft = () => getDummyNft();  dummyProfile = () => getDummyProfile();  gradientBackgroundImage = (image: string) => `      linear-gradient(      to bottom,      rgba(255, 255, 255, 0.6),      rgba(255, 255, 255, 1)    ),    url(${image})  `;  redirectToExplore = () => this.router.navigate(['/explore']);}