import { ECardImageHeightSize, ICardConfig } from '@shared/components/card/domain/config.interface';import { getRandomInt }                      from '@shared/utils/main.utils';class LandingUtils {  static getFakeCollections = (): ICardConfig[] => Array.of(this.getFakeCollection(), this.getFakeCollection(), this.getFakeCollection());  static getFakeCollection = (): ICardConfig => ({    title: 'Collection 1',    description: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',    textAlign: 'center',    image: {      path: 'https://picsum.photos/1280?random=' + getRandomInt(1, 20),      alt: 'Collection 1',      height: ECardImageHeightSize.extraLarge    },    action: {      label: 'Live',      action: ($event, emitter) => {        emitter?.emit('live');      },    }  });  static getRandomImageList(amount: number, resolution: string): string[] {    const images = [];    for (let i = 0; i < amount; i++) {      images.push(getRandomImage(resolution));    }    return images;  }  static getRandomImage = (resolution: string = '1280'): string => `https://picsum.photos/${ resolution }?random=${ getRandomInt(1, 20) }`;}export const getFakeCollections = LandingUtils.getFakeCollections;export const getRandomImage = LandingUtils.getRandomImage;export const getRandomImageList = LandingUtils.getRandomImageList;