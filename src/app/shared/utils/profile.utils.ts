import { IProfile } from '@shared/models/profile.interface';export const getProfile = (): IProfile => {  const profile = localStorage.getItem('profile');  return profile ? JSON.parse(profile) : null;};export const getDummyProfile = (): IProfile => {  return {    id: 1,    name: 'John Doe',    email: 'test@mail.cl',    avatar: 'https://randomuser.me/api/portraits/men/75.jpg',    created_at: new Date(),    updated_at: new Date(),  }};// TODO: fetch random from https://randomuser.me api user casting to IProfile object